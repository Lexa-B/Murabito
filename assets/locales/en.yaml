# English UI strings, one `key: text` per line. Keys are grouped by screen, as
# `menu.quit`; a key is added here when the text that uses it is, and ja.yaml must carry
# the same keys (a test fails otherwise). Compiled into the game: editing needs a rebuild.
menu.settings: Settings
menu.resume: Resume
menu.quit: Quit
settings.title: Settings
settings.pan_speed: Pan speed
settings.language: Language
settings.back: Back
