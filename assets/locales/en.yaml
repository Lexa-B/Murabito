# English UI strings. Keys are grouped by screen; see assets/locales/ja.yaml for the
# Japanese catalogue, which must carry the same keys.
menu.settings: Settings
menu.resume: Resume
menu.quit: Quit

settings.title: Settings
settings.back: Back
settings.pan_speed: Pan speed
settings.language: Language
# The language button offers the *other* language, so it is named in its own script and
# is never translated: someone who can't read the current language still recognises it.
settings.language_toggle: 日本語

# The F3 debug screen. Developer-facing, but localised like everything else.
debug.senses: Senses

# Taxonomy keys, namespaced by the axis they belong to. 諸法.yaml holds no display
# names on purpose: the key is the identity, and this is what a reader sees.
分類.狐: Fox
分類.兎: Rabbit
